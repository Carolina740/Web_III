@page "{id:int}"
@model WebApplication2.Pages.Mascotas.DeleteModel
@{
    ViewData["Title"] = "Eliminar Mascota";
}

<nav aria-label="breadcrumb" class="breadcrumb-vet">
    <ol class="breadcrumb mb-0">
        <li class="breadcrumb-item"><a asp-page="/Index"><i class="bi bi-house"></i></a></li>
        <li class="breadcrumb-item"><a asp-page="Index">Mascotas</a></li>
        <li class="breadcrumb-item active">Eliminar</li>
    </ol>
</nav>

<div class="row justify-content-center mt-2">
    <div class="col-lg-6">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>Confirmar Eliminación
            </div>
            <div class="card-body p-4">
                <div class="text-center mb-4">
                    <div style="font-size:3.5rem;">⚠️</div>
                    <h5 class="mt-2">¿Eliminar a <strong>@Model.Mascota?.Nombre</strong>?</h5>
                    <p class="text-muted">Esta acción no se puede deshacer.</p>
                </div>

                @if (Model.TieneCitas)
                {
                    <div class="alert alert-warning d-flex align-items-center gap-2">
                        <i class="bi bi-exclamation-circle-fill fs-5"></i>
                        <div>
                            Esta mascota tiene <strong>@Model.CantidadCitas cita(s)</strong> registrada(s).
                            Elimine las citas asociadas antes de eliminar la mascota.
                        </div>
                    </div>
                    <div class="d-flex justify-content-end">
                        <a asp-page="Index" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left me-1"></i>Volver
                        </a>
                    </div>
                }
                else
                {
                    <dl class="row mb-3">
                        <dt class="col-5 text-muted">Nombre</dt>
                        <dd class="col-7">@Model.Mascota?.Nombre</dd>
                        <dt class="col-5 text-muted">Especie</dt>
                        <dd class="col-7">@Model.Mascota?.Especie</dd>
                        <dt class="col-5 text-muted">Raza</dt>
                        <dd class="col-7">@Model.Mascota?.Raza</dd>
                        <dt class="col-5 text-muted">Propietario</dt>
                        <dd class="col-7">@Model.Mascota?.Propietario?.NombreCompleto</dd>
                    </dl>

                    <form method="post" data-confirm="¿Estás segura de que deseas eliminar a @Model.Mascota?.Nombre? Esta acción no se puede deshacer.">
                        @Html.AntiForgeryToken()
                        <div class="d-flex gap-2 justify-content-end">
                            <a asp-page="Index" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left me-1"></i>Cancelar
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-trash-fill me-1"></i>Sí, eliminar
                            </button>
                        </div>
                    </form>
                }
            </div>
        </div>
    </div>
</div>
